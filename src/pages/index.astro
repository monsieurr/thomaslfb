---
import BaseLayout from '../layouts/BaseLayout.astro';
const pageTitle = "Thomas L. | Product & Data";
---
<BaseLayout title={pageTitle}>
  <section class="intro-section">
    <span class="eyebrow">Home</span>
    <h1>Hello, I'm Thomas.</h1>
    
    <div class="bio-content">
      <p>
        I am a <strong>Product Manager</strong> specialized in <strong class="highlight">Data, AI, and Sustainability</strong>. 
        I bridge the gap between technical engineering and business impact to build products that matter in the Green Tech space.
      </p>
      <p>
        With a background as a <strong>Software Engineer</strong>, I bring technical rigor to product discovery and delivery. 
        Beyond the roadmap, I am an active sustainability advocate, facilitating <em>Climate</em> and <em>Digital</em> workshops, and a <strong>creative soul</strong> with a 15-year passion for photography that fuels my eye for UX/UI.
      </p>
    </div>

    <div class="cta-container">
      <!-- These now use the global button styles -->
      <a href="/projects" class="btn">Explore my Work</a>
      <a href="https://www.linkedin.com/in/thomas-lefebvre-product-impact/" target="_blank" rel="noopener noreferrer" class="btn btn-ghost">
        Connect on LinkedIn ↗
      </a>
    </div>
  </section>
</BaseLayout>

<style>
  /* =========================================
     1. DESKTOP LOCK (The "No Scroll" Logic)
     We use :global() to override global.css styles
     only for this page.
  ========================================= */
  @media (min-width: 768px) {
    /* Lock the screen height */
    :global(body) {
      height: 100vh;
      overflow: hidden; /* This kills the scrollbar */
      display: flex;
      flex-direction: column;
    }

    /* Remove the huge global margins that caused the overflow */
    :global(header) { margin-bottom: 0 !important; }
    :global(footer) { margin-top: 0 !important; padding-top: 2rem !important; padding-bottom: 2rem !important; }
    :global(main) { padding: 0 !important; }

    /* Make Main take all available space and center content */
    .home-main {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center; /* Vertical Center */
      align-items: center;
    }
  }

  /* =========================================
     2. CONTENT STYLING
  ========================================= */
  .intro-section {
    max-width: 700px;
    width: 100%;
    padding: 0 1.5rem;
    /* On mobile, we keep the margin. On desktop, flex handles it. */
    margin: 1rem auto 0; 
  }

  /* Reset margin for desktop since we use Flex centering */
  @media (min-width: 768px) {
    .intro-section { margin: 0 auto; }
  }

  .eyebrow {
    font-family: var(--font-mono);
    color: var(--accent);
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    font-weight: 700;
    margin-bottom: 1rem;
    display: block;
  }

  h1 {
    font-size: 3rem;
    margin-bottom: 2rem;
    line-height: 1.1;
    color: #111;
  }

  .bio-content p {
    font-size: 1.15rem;
    line-height: 1.7;
    color: #444;
    margin-bottom: 1.5rem;
  }

  .highlight { color: var(--accent); }

  /* =========================================
     3. BUTTONS
  ========================================= */
  .cta-container {
    margin-top: 3rem;
    display: flex;
    gap: 1rem;
    align-items: center;
  }

  /* =========================================
     4. MOBILE RESPONSIVE
  ========================================= */
  @media (max-width: 768px) {
    /* Allow scrolling on mobile */
    :global(body) { height: auto; overflow: auto; }
    
    h1 { font-size: 2.5rem; }
    
    .cta-container {
      flex-direction: column;
      width: 100%;
    }
    .btn { width: 100%; }
  }
</style>
